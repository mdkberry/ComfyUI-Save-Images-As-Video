[FFMPEG]
# Path to the folder containing the ffmpeg executable (e.g., /path/to/ffmpeg/bin or C:\ffmpeg\bin).
# Can be an absolute path or a path relative to this custom node's directory.
# Leave empty or comment out to use ffmpeg from this node's "ffmpeg_bin" subfolder first, 
# then from the system PATH.
# Example: custom_ffmpeg_path = /usr/local/ffmpeg/bin
# Example (relative): custom_ffmpeg_path = my_other_ffmpeg_folder
custom_ffmpeg_path = 
